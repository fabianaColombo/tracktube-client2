export const selectExplore = (state) => state.explore;

export const selectExploreIds = (state) => state.explore;
// export const selectFavorites = (state) => state.explore.favoriteIds;
// console.log("select favorites", selectFavorites);
